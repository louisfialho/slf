<!-- BEGINNING OF NAVBAR -->
<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <%= link_to "shelf", root_path, class: "logo" %>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <% if @shelf %>
      <%= link_to image_tag("Plus.png", width: 40).html_safe, shelf_path(@shelf), class: 'rotated' %>
    <% elsif @space %>
      <%= link_to image_tag("Plus.png", width: 40).html_safe, space_path(@space), class: 'rotated' %>
    <% end %>
  </div>
</div>
<!-- END OF NAVBAR -->


<% if Item.find(params[:item_id]).medium == "book" %>
  <h1 class="question">What is the status of this new 📕 ? </h1>
<% elsif Item.find(params[:item_id]).medium == "podcast" %>
  <h1 class="question">What is the status of this new 🎙 ? </h1>
<% elsif Item.find(params[:item_id]).medium == "video" %>
  <h1 class="question">What is the status of this new 📺 ? </h1>
<% elsif Item.find(params[:item_id]).medium == "web" %>
  <h1 class="question">What is the status of this new 💻 ? </h1>
<% else %>
  <h1 class="question">What is the status of this new object ? </h1>
<% end %>







<% if @shelf %>

  <%= simple_form_for @item, method: :put, url: wizard_path do |f| %>
        <div class='choices inp'> <%= f.input :status, label: false, checked: 'not started', collection: [['🙅 Not started', 1], ['🧑‍💻 In progress', 2], ['💆 Finished', 3]], as: :radio_buttons, input_html: { class: "tag-selector" }, item_wrapper_class: 'tag-item' %> </div>
        <%= f.hidden_field :shelf_id, :value => @shelf.id %>
        <div class='buttonHolder'> <%= f.submit "Next", class: "add-btn" %> </div>
  <% end %>

<% elsif @space %>

  <%= simple_form_for @item, method: :put, url: wizard_path do |f| %>
        <div class='choices inp'> <%= f.input :status, label: false, checked: 'not started', collection: [['🙅 Not started', 1], ['🧑‍💻 In progress', 2], ['💆 Finished', 3]], as: :radio_buttons, input_html: { class: "tag-selector" }, item_wrapper_class: 'tag-item' %> </div>
        <%= f.hidden_field :space_id, :value => @space.id %>
        <%= f.hidden_field :parent_id, :value => @space.id %> <!-- apparently for Pundit -->
        <div class='buttonHolder'> <%= f.submit "Next", class: "add-btn" %> </div>
  <% end %>

<% end %>
